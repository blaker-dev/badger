import './stylesheets/app.css';
import { PaperManager } from './PaperManager';

export const BoardManager: React.FC = () => {
    return (
        <div className="">
            <PaperManager/>
        </div>
    );
};
